import { Unauthcustomerdetails } from './unauthcustomerdetails';

describe('Unauthcustomerdetails', () => {
  it('should create an instance', () => {
    expect(new Unauthcustomerdetails()).toBeTruthy();
  });
});
